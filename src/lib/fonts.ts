import { Fredoka, Poppins, Tilt_Prism } from "next/font/google";

export const tiltPrismFont = Tilt_Prism({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});

export const poppinsFont = Poppins({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});

export const fredokaFont = Fredoka({
  subsets: ["latin"],
  weight: ["500"],
  display: "swap",
});
